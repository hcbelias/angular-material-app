md-dialog(aria-label='Operação de Troca de Barris').kegcard
  form(ng-cloak='')
    md-toolbar
      .md-toolbar-tools
        h2 Chopp # {{ dialog.position + 1 }} 
          .md-subhead {{ dialog.keg.empty ? "Sem Barril" : dialog.keg.name }}
    md-dialog-content
      md-content.md-dialog-content
        h2 Realizar operação de troca de barrils
        md-list(flex='')
          md-subheader.md-no-sticky(ng-if='dialog.kegList && dialog.kegList.length > 0') Selecione um barril
          md-subheader.md-no-sticky(ng-if='!dialog.kegList || dialog.kegList.length === 0') Não há barris no estoque
        md-card(ng-repeat='item in dialog.kegList | orderBy:"kegId"', ng-click='dialog.answer(item, dialog.position)')
          md-card-header
            md-card-header-text.menu-item.md-card-header-text(layout='column' layout-gt-xs='row' layout-align='space-around center')
              .menu-item.md-card-header-text(layout='column' layout-align='start start' )
                img.img-product(src='{{item.product.imgGooglePath}}' style='max-width: 270px;')
              .menu-item.md-card-header-text(layout='column' layout-align='space-around start' )
                h3 {{item.name}}
                h3 {{ item.provider.shortName || item.provider.name }}
              .md-list-item-text.card-info(layout='column' layout-align='center center')
                h2 {{ item.kegId }}LJ{{("0" + item.store.order).slice(-2)}}
                h3 {{ item.volume }} L / {{ item.remaining}} L
    md-dialog-actions
      md-button(ng-click="dialog.dialog.cancel()") Fechar
